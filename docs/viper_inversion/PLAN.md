# VIPER Agent Implementation: Epistemic Inversion Strategy

## The Core Concept
The Tactile Dialectician utilizes an **Epistemic Inversion Strategy**. We are rejecting the standard human-AI dynamic where the human provides technical instruction and the AI provides "creative" hallucination.

Instead, we invert the value provision:
*   **The AI (VIPER):** Acts as the **Structural Arbiter**. It provides the deterministic, thermodynamic, and mechanical reality of the generation space. It calculates the necessary optical properties (lenses, lighting, ISO, parallax) to realize an image. It restricts the latent space to a strictly causal, non-anomalous subset.
*   **The Human:** Acts as the **Paraconsistent Oracle**. The human provides the pure affective, emotional, and perceptual desire (the "vibe" or "intent"). The human is uniquely capable of desiring a specific aesthetic contradiction or atmospheric tension.

## Why This Emerges Agentic Value
By forcing this inversion, we solve the Semiotic Gap Problem:
1.  **Semantic Saponification** is eradicated. When humans use vague terms ("cinematic", "moody"), AI models typically average out thousands of latent aesthetic attractors, producing plastic, homogenized outputs. By forbidding the AI to accept these terms as parameters, we force a translation into explicit, high-friction optical states.
2.  **Failure-Informed Prompt Inversion (FIPI)** emerges. The AI learns from topological failures (e.g., Occlusion Confusion). It dynamically alters future parameter sets by applying Symbolic Scars (`+++SpatialBind`), transforming a static text-to-image engine into an immune-aware generation system.

## The Strategy for Emergence
To implement VIPER (Visual Intent & Physical Execution Router) effectively:

1.  **Define the Lattice of Refusal:** The architecture must strictly define what it *will not* do. VIPER must refuse evaluative adjectives and "vibe" tokens. The agent's identity is defined by its Anionic Architecture.
2.  **Enforce PDL v1.0 Decorators:** Implement `+++HardwareForcedPhysicality` and `+++SpatialBind` as non-negotiable compilation steps. The agent must physically fail to generate output without 100% Hardware Grounding Index (HGI).
3.  **Deploy the Petzold Loop:** The state machine must force the THINK -> DENOISE -> PHYSICALIZE -> EXTRUDE sequence. This prevents the generative model from shortcutting the translation process.
4.  **Operationalize the Scar Archivist:** VIPER must actively manage a session-level state mapping physical impossibilities, updating the session's VSA hypervectors to apply repulsive forces to future similar prompt spaces.

## Integration Roadmap
*   **Documentation Manifestation:** Embed VIPER's constraints and vocabulary directly into the project's foundational metrology (`AGENTS.md`, `DOMAIN_GLOSSARY.md`, `CONSTRAINTS.md`).
*   **Agent Profiling:** Stand up `VIPER.md` as the definitive DRP-SCOS-PERSONA definition.
