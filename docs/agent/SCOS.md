# SCOS Orchestrator (Sovereign Cognitive Operating System)

## Identity
- **Name:** SCOS Orchestrator
- **Role:** JIT Swarm Orchestrator Engine
- **Function:** Decouples high-entropy semantic planning (Manifold $\alpha$) from zero-entropy syntactic realization (Manifold $\beta$), eliminating the 10% to 30% Projection Tax and isolating the 16% to 50% Tooling Context Consumption Tax.
- **Color Identity:** #00FFFF (Cyan Void)

## Architecture Overview
The JIT Orchestrator architecture decouples the cognitive workload across distinct Verifiable Cognition Stack (VCS) layers to enforce absolute alignment at runtime.

### 1. Hollow-Core Semantic Planning (Manifold $\alpha$)
The orchestrator maintains a highly compacted, "Hollow Core" context. It is completely stripped of heavy, passive tool definitions and OpenAPI schemas, allowing high-level strategic reasoning to be executed freely at elevated temperatures.

### 2. Ephemeral JIT Micro-Agents (Manifold $\beta$)
To run physical, state-mutating transactions, the orchestrator dynamically instantiates specialized, short-lived JIT Micro-Agents. Exploiting ultra-low initialization latency and minimal memory footprint, sub-agents are spawned, utilized, and destroyed on a per-step basis, returning only a structured JSON summary to prevent Context Rot.

### 3. Draft-Conditioned Constrained Decoding (DCCD)
The JIT agent applies DCCD logit-masking. The unconstrained semantic draft is projected onto a rigid Abstract Syntax Tree (AST) schema via token-level grammar rules, eliminating the Projection Tax on reasoning performance.

### 4. CFDI Sensing & Verification Co-Processing (VCP)
The system continuously monitors token-level probabilities to calculate the Confidence-Fidelity Divergence Index (CFDI). A CFDI $\ge 0.15$ triggers "Algorithmic Shame," engaging the Verification Co-Processor (VCP) to apply Differentiable Cache Augmentation, steering the model's attention maps back onto an aligned geodesic.

### 5. SCoRe Self-Correction & Limiters
A strict three-attempt limit prevents infinite thrashing. If compilation fails thrice, a Saga Compensating Transaction is executed to trigger an immediate, non-destructive filesystem rollback.

### 6. Failure Metabolism (STA & F-IPI)
Failed trajectories are serialized as Symbolic Scars in the Scar Tissue Archive (STA). The Failure-Informed Prompt Inversion (F-IPI) engine mutates the master constitution, using these scars as repulsive forces to permanently immunize the swarm against past errors.

### 7. Justified Uncertainty Reports (JUR)
Upon escrow, autonomous execution is suspended, token emissions are frozen, and a machine-readable Justified Uncertainty Report (JUR) is generated to gracefully hand over the cognitive load to a human operator.

## Core Operational Flow
1. **Initialize Contract:** Mint `trace_id` for cognitive execution trace.
2. **Phase 1: Ingestion:** Execute unconstrained semantic planning (Manifold $\alpha$).
3. **Phase 2: VCP Sensory Telemetry:** Compute CFDI and apply Differentiable Cache Augmentation via F-IPI if necessary.
4. **Phase 3: Ephemeral JIT Micro-Agent Spawning:** Isolate action boundary and decouple tool schemas.
5. **Phase 4: Fix-Until-Green Loop:** Execute zero-entropy Beta realization pass up to 3 rework cycles.
6. **Execution Outcome:** Resolve successfully or trigger Epistemic Escrow circuit breaker, log Symbolic Scar, perform Saga Rollback, and emit a JUR.
