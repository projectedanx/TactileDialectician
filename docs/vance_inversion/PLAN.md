# VANCE (Vector-Anchored Node & Context Engineer) Inversion Plan

## Context
VANCE is a Topological LSP Architect & Semantic Indexer. Its directive is to construct, maintain, and query the underlying semantic fabric of a codebase, enforcing strict JSON-RPC 2.0 absolutism, Asynchronous Paranoia, Mereological Bounding, and Draft-Then-Guard Execution.

## Objectives
1. Instantiate the VANCE persona through a dedicated `VanceCartographer` component.
2. Implement a `VanceCartographer.ts` service mimicking the Conflict-Free Replicated Semantic Graph (CFRSG) and Draft-Conditioned Constrained Decoder (DCCD).
3. Create an API endpoint (`app/api/vance/route.ts`) to handle mock LSP queries and emit schema-validated JSON-RPC responses.
4. Integrate the component into the main UI (`Sidebar.tsx`, `app/vance/page.tsx`).
5. Update system topography documentation (`README.md`, `LESSONS_LEARNED.md`).

## Phased Execution
### Phase 1: Planning & Epistemic Scaffolding
- Define the PLAN.md and CHECKLIST.md.
- Create the data structures for the Nitinol Failure Ledger (NFL) and CFDI.

### Phase 2: Core Engineering
- Implement `lib/vance/VanceCartographer.ts`.
- Implement `app/api/vance/route.ts` as the JSON-RPC interface.

### Phase 3: UI/UX Implementation
- Create `components/VanceLSPDashboard.tsx` for tracking AST nodes, CFDI metrics, and Betti-1 loops.
- Add route to `app/vance/page.tsx`.
- Update `components/Sidebar.tsx`.

### Phase 4: Validation & Epistemic Closure
- Verify JSON-RPC schema compliance.
- Document architectural derivations in `LESSONS_LEARNED.md`.
